<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Network Social - Página de pesquisa</title>
  <link rel="stylesheet" href="/css/search.css" />
</head>
<body>
  <header class="header">
    <div class="logo">Network Social</div>
    <nav class="nav">
      <a href="/home">Início</a>
      <a href="/home/search">Procurar usuarios</a>
      <a href="/home/messages">Mensagens</a>
      <a href="/home/profile">Perfil</a>
    </nav>
  </header>

  <main class="main-content">
    <aside class="sidebar">
      <div class="profile">
        <img id="profileImage" src="<%= profile && profile.img ? profile.img : 'https://via.placeholder.com/120' %>" alt="Foto de perfil">
        <h2><%= user.name %></h2>
      </div>

</div>
    </aside>

   <section class="feed">
  <form action="/home/search" method="get">
    <input type="search" name="q" placeholder="Buscar usuários..." required />
    <button type="submit">Buscar</button>
  </form>

  <h1>Resultados para "<%= term %>"</h1>

  <% if (usersFound.length === 0) { %>
    <p>Nenhum usuário encontrado.</p>
  <% } else { %>
    <ul>
      <% usersFound.forEach(u => { %>
        <li>
          <img src="<%= profileMap[u._id] || 'https://via.placeholder.com/50' %>" width="50" />
          <a href="/home/user/<%= u._id %>"><%= u.name %></a>
        </li>
      <% }) %>
    </ul>
  <% } %>
</section>

  </main>
</body>
</html>
